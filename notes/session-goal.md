[x] Add per-point depth to Contact (or a small ContactPoint struct bundling position + depth)
[x] Compute depth for each point coming out of ClipFaceFace
[x] Add the area-maximization reduction (only needed on the face-face path -corner and edge paths return just 1 point)
